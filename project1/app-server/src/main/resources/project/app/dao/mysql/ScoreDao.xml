<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.app.dao.ScoreDao">

  <resultMap type="score" id="scoreMap">
    <result column="score_no"   property="no"/>
    <result column="sub1"       property="sub1"/>
    <result column="sub2"       property="sub2"/>
    <result column="sub3"       property="sub3"/>
    <result column="sub4"       property="sub4"/>
    <result column="grade"      property="grade"/>
    <result column="scholar"    property="scholar"/>
    
    <association property="student" javaType="student">
      <id     column="student_id"   property="no"/>
      <result column="name"         property="name"/>
    </association>
    
  </resultMap>

  <insert id="insert" parameterType="score">
    insert into score(student_id,sub1,sub2,sub3,sub4,grade,scholar)
    values(#{student.no},#{sub1},#{sub2},#{sub3},#{sub4},#{grade},#{scholar})
  </insert>
  
  <select id="findAll" resultMap="scoreMap">
    select
      s.score_no,
      s.student_id,
      t.name,
      s.sub1,
      s.sub2,
      s.sub3,
      s.sub4,
      s.grade,
      s.scholar
    from
      score s inner join student t on s.student_id=t.student_no
    order by 
      s.student_id
  </select>
  
  <select id="findBy" parameterType="int" resultMap="scoreMap">
    select
      s.score_no,
      s.student_id,
      t.name,
      s.sub1,
      s.sub2,
      s.sub3,
      s.sub4,
      s.grade,
      s.scholar
    from
      score s inner join student t on s.student_id=t.student_no
    where
      score_no=#{no}
  </select>
  
  <update id="update" parameterType="score">
	  update score set
	    sub1=#{sub1},
	    sub2=#{sub2},
	    sub3=#{sub3},
	    sub4=#{sub4},
	    grade=#{grade},
	    scholar=#{scholar}
	  where 
	    score_no=#{no}
	    and student_id=#{student.no}
  </update>
</mapper>