<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.app.dao.MemberDao">

  <resultMap type="member" id="memberMap">
    <id     column="no"           property="no"/>
    <result column="type"         property="type"/>
    <result column="name"         property="name"/>
    <result column="email"        property="email"/>
    <result column="password"     property="password"/>

    <association property="subject" javaType="subject">
      <id     column="student_id"   property="subjectno"/>
      <result column="name"         property="name"/>
    </association>
  </resultMap>

  <insert id="insert" parameterType="member">
    insert into member(type,name,email,password)
    values(#{type},#{name},#{email},sha1(#{password}))
  </insert>
  
  <select id="findAll" parameterType="int" resultMap="memberMap">
    select
      no,
      name,
      email
    from
      member
    where
      type=#{type}
  </select>
  
  <select id="findBy" parameterType="int" resultMap="memberMap">
    select
      no,
      name,
      email
    from
      member
    where
      no=#{no}
  </select>
  
  <select id="findByEmailAndPassword" parameterType="member" resultMap="memberMap">
    select
      no,
      name,
      email
    from
      member
    where 
      email=#{email}
      and password=sha1(#{password})
  </select>
  
  <update id="update" parameterType="member">
	  update member set
	    name=#{name},
	    email=#{email},
	    password=sha1(#{password})
	  where
        no=#{no}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from member
    where
      no=#{no}
  </delete>
</mapper>